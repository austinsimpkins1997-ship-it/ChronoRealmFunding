# DROP-IN INSTRUCTIONS

This package is intentionally additive. It does not replace the repository's existing `index.html`, `README.md`, or funding-page content.

## Fastest path

1. Open the `ChronoRealmFunding` repository on your computer.
2. Extract this ZIP.
3. Copy the CONTENTS of this package directly into the repository root.
4. Commit and push.

New files/folders:

- `PUBLIC_EVIDENCE.md`
- `public-evidence/`
- `OPTIONAL_FUNDING_PAGE_LINK_SNIPPET.html`
- `OPTIONAL_FUNDING_PAGE_LINK_SNIPPET.md`
- `GITHUB_RELEASE_BODY_v1.5.md`
- `VERIFY_PUBLIC_RELEASE.py`
- `VERIFY_PUBLIC_RELEASE.bat`
- `PUBLIC_RELEASE_SHA256SUMS.txt`

## Link the existing funding page

For an HTML page, use:
`OPTIONAL_FUNDING_PAGE_LINK_SNIPPET.html`

For a Markdown page, use:
`OPTIONAL_FUNDING_PAGE_LINK_SNIPPET.md`

The relative evidence URL is:
`./public-evidence/`

## Intended Pages URL

If `public-evidence/` is placed in the same GitHub Pages source directory as the existing funding page, the intended public path is:

`https://austinsimpkins1997-ship-it.github.io/ChronoRealmFunding/public-evidence/`

If the repository publishes Pages from `docs/` instead of the repository root, place these public-facing files under that same Pages source directory.

## Optional GitHub Release

Use `GITHUB_RELEASE_BODY_v1.5.md` as the release description and attach:

- `ChronoRealm_WE_FINAL_v1.5_PUBLIC_EVIDENCE_BUNDLE_2026-08-12.zip`
- its `.sha256`
- `ChronoRealm_v1.5_Public_Evidence_One_Page.pdf`

## Claim discipline

Do not relabel local PASS results as independent certification.
Do not remove unresolved questions merely because later tests pass.
Public evidence should supersede or annotate prior evidence rather than silently rewrite history.
